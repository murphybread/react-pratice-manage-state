import React from "react";
import ImageLoadingState from "../components/ImageLoadingState";

export default {
  title: "Button/ImageLoadingState",
  component: ImageLoadingState,
};

const Template = () => <ImageLoadingState />;

export const Default = Template.bind({});
Default.storyName = "Default";
